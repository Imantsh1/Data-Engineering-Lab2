name: 'lab2_pipeline'
version: '1.0.0'
config-version: 2

# Update this to match what you wrote in profiles.yml
profile: 'playstore_profile'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]

models:
  lab2_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table